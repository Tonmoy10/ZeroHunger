@model IEnumerable<ZeroHunger.db.Admin>

@{
    ViewBag.Title = "ViewAdmin";
}
<br />
<a href="/Main/login">
    <button style="float:right" class="btn-warning col-lg-1">Login</button>
</a>
<a href="/Main/Home">
    <button style="float:right" class="btn-warning col-lg-1">Add User</button>
</a>

<h2>ViewAdmin</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Admin ID
        </th>
        <th>
            Name
        </th>
        <th>
            Email
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.a_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.a_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                @Html.ActionLink("Delete", "DeleteAdmin", new { id = item.a_id })
            </td>
        </tr>
    }

</table>
